package pack;

/*
* Get "getAllClasses()" to also search for interfaces (they're also Class<Dynamic>)
* While we're at it, fix the casting to List<T>?
* Write some tests (interface C, implemented by sub1.Sub1TypeANumber1 and sub2TypeBNumber2)
* Create UFAdminModule interface (w/ build macro to check constructor args)
* in UFAdminController, use getAllClasses( UFAdminModule ) interface
* Create an EasyAuthAdminController (placeholder for now)
	* List users
	* Show user, list permission
* Move DBAdminController to ORM?
*/
interface InterfaceC {

}
